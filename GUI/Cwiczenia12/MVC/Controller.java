package GUI.Cwiczenia12.MVC;

/*
import javafx.event.*;

public class Controller {

    private final View view;
    private final Model model;

    Controller(Model model, View view) {
        this.model = model;
        this.view = view;
    }

    public void initView() {
        view.getSumLabel().setText(String.valueOf(model.getX()));
    }

    public void initController() {

        view.getSumButtton().addEventHandler(
                /* event type
                ActionEvent.ACTION ,
                /* event handler/"listener"
                (event) -> {
                    int newX = Integer.parseInt(view.getTextField().getText());
                    model.setX(newX);
                    view.getSumLabel().setText(String.valueOf(model.getX()));
                }
        );


        // convenience method
        view.getClearButton().setOnAction(
                event -> {
                    view.getTextField().setText("0");
                    model.resetX();
                    view.getSumLabel().setText("0");
                }
        );
    }
}
*/
